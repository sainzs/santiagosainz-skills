# OpenCode Adapter

Use OpenCode's native task and command surfaces.

- Use `todowrite` for active implementation tasks.
- Prefer `/plan` for multi-step local planning and `/verify` after edits.
- Use issue-oriented skills only when the user asks to create external issues.
- Install destination: `~/.agents/skills`.
